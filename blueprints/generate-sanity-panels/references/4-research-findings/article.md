# Research Findings — article

Algolia subagent: run when `{brief}` names author or product theme.

Example: author biography brief → filter author field from `{brief}`, summarize related books into `products[]`.

Skip Algolia when pure editorial brief with no catalogue tie.
