# Desired Content — article

Seed identifier: article slug or doc id. **Seed surface URL** = canonical article URL — never link CTAs back to it.

Long-form OK. Hero + 8–12 body blocks with **final** copy.

Pull quotes verbatim from `findings.quotes` when confidence high — use testimonial / quote panels, not `richText` panel type.

Catalogue weave: 2–4 natural mentions max unless brief asks more — **outbound** product/maker URLs only.
