# Desired Content — email

Seed identifier: campaign name or template slug. Landing URL from brief = **seed surface** — do not repeat as primary CTA.

Subject + preheader in `narrativeNote`. Short blocks. One primary CTA slice.

`product_row` when `products[]` present — title/maker/**outbound** link/cover; no price/stock; no raw internal IDs.
