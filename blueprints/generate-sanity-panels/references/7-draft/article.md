# Draft — article

Panel field: `richText[]` embeds on `article` doc. Seed identifier: slug or doc id. Seed surface = canonical article URL — no links back.

Use gallery `_type`s allowed for article cache — never `markdown` or `richText` panel types. See `references/7-draft/shared.md`.
