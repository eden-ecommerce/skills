# Draft — email

Slice / block array per tenant schema. Seed identifier: campaign slug from brief. Landing URL = seed surface — no primary CTA back to it.

Map slices using gallery + `.cache/*-panels.json` for tenant. Never `markdown` or `richText` panel types. See `references/7-draft/shared.md`.

Preview URL from `domainSnapshot.urls.preview`.
