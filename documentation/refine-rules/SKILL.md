---
name: refine-rules
description: "Critically review and refine PROJECT_RULES.md into clear, code-led hidden knowledge gaps."
agents:
  - cursor
---

# Rule: Fix Project Rules (Caveman Ultra Mode)

Use this when fix `@PROJECT_RULES.md`, `.cursor/rules/` markdown, or `.mdc` files.

If user give specific file, ONLY fix that file. Make file dumb simple. AI must see secret code traps.

## Goal
Show invisible system traps. If AI can look at code or internet to find answer, **DELETE IT**. Only keep hidden tribe secrets.

## Rules
* **No Bloat:** No big words. No long text. No links. No `docs/` files.
* **Code First:** Show bad code, show good code. One short sentence say why.
* **Don't Chain AI:** No style rules. No lint rules. Only unblock AI from breaking hidden things.

---

## Output Structure

```markdown
# Project Rules: Hidden Traps

> Secret knowledge only. Not found in code search. Not found on internet.

* **[Trap Name]**
