# Definition — email

Delta for `target=email`.

- No extra requirements — narrative `{brief}` is enough
- Surface must be `email` in `domainSnapshot.surfaces`
- Identifier = campaign or template slug from brief (optional; not blocking)
- Slices not page panels — field name from target + tenant schema
- Multi-SKU brief: flag `commerceSlice: true` in bundle for stage 4
