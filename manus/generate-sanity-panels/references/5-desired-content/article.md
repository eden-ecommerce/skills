# Desired Content — article

Long-form OK. Hero + 8–12 body **blocks** with final copy (not outlines).

Pull quotes verbatim from `findings.quotes` when confidence high into `quote_block`s.

Product weave: 2–4 natural catalogue mentions max unless brief asks more — human labels + URLs in copy (no raw internal IDs).
