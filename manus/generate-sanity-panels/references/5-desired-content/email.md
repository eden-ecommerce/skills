# Desired Content — email

Blocks = email slices with **final** subject-ready copy. Subject + preheader in `narrativeNote`.

Short blocks. One primary `cta_block`. `product_row` when `products[]` present (title/maker/link/cover — no price/stock text; human labels + URLs only, no raw internal IDs).
