# Audit — article

## Overlay checklist rows (add to shared)

| id | Check |
|---|---|
| `readability` | Section length, heading hierarchy scannable |
| `quote_verbatim` | Pull quotes match `findings` verbatim or are removed |

No product-specific matrix rows unless article includes comparison modules.
